# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build pokemon-master-api-gateway`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t pokemon-master-api-gateway`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system pokemon-master-api-gateway && \
          adduser --system -G pokemon-master-api-gateway pokemon-master-api-gateway

COPY dist/apps/pokemon-master-api-gateway pokemon-master-api-gateway/
RUN chown -R pokemon-master-api-gateway:pokemon-master-api-gateway .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix pokemon-master-api-gateway --omit=dev -f install

CMD [ "node", "pokemon-master-api-gateway" ]
